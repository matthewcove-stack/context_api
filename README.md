# context_api

FastAPI service that mirrors key Notion databases into Postgres and serves compact context packs for LLM reasoning.

Phase 1 scope (initial): Projects + Tasks mirroring with manual sync endpoints.
